基础模块
  时间轴
  空间网格
  可视化
衍生模块
  区域 空间容器 存放体 场 波
  代理对象特性
    区域连接
    接收 能量 信息
  代理对象继承树
    代理对象(存在,销毁)
    时间代理对象(行动)
    不动代理对象(位置)
    时空代理对象(对象, 移动, 形变) 波代理对象(传递)
    体代理对象(对象场, 对象波, 接收) 场代理对象(区域连接, 传递)
  对象
    场 存在时间长,能够移动 与范围内对象互动
    波 存在时间短,不会移动 与扫过的对象互动
    体
      对象场 对象本身的存在, 动作
      对象波 对象发出的能量或信息流
      状态 特定行动时会触发的效果或判定
