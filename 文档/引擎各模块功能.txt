基础模块
  时间轴
  空间网格
  可视化
衍生模块
  区域 空间容器 存放体 场 波
  对象继承树
    代理对象(存在,销毁)
    时间代理对象(行动)
    不动代理对象(位置)
    时空代理对象(对象 移动 形变)
    辐射 连接 传递
    接收 能量 信息
  对象
    场 存在时间长,能够移动 与范围内对象互动 移动 行动 连接 传递
    波 存在时间短,不会移动 与扫过的对象互动 传递
    体 行动 移动 形变 销毁 辐射 传递 接收
      对象场 对象本身的存在, 动作
      对象波 对象发出的能量或信息流
      状态 特定行动时会触发的效果或判定
